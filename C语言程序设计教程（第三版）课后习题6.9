#include<stdio.h>

int main()
{
	int m, n;
	scanf("%d%d", &m, &n);
	double h = m, sum = m,ret=0;
	while (n--)
	{
		sum += 2*ret;
		h = h / 2.0;
		ret = h;
	}
	printf("%.2lf %.2lf", h, sum);
	return 0;
}
